---
updated: 2026-08-30T10:17
---

- (decision) Architecture: single workflow skill gzp-pipeline, no persona/multi-agent split, per ideation plan at skills/reports/zoho-github-taskflow-plan.md.
- (decision) Working-state strategy: structured working artifact only (no memlog-style decision trail needed at runtime) — a JSON session-state file managed by scripts/session_state.py, since the value is the current pipeline position (what), not reasoning (why).
- (decision) customize.toml: declined (default no) — the three config values (gzp_mcp_name, gzp_zoho_project_name, gzp_default_bill_status) are module config via _bmad/config.yaml, not a customization surface.
- (decision) Missing config degradation: if gzp_mcp_name/gzp_zoho_project_name absent from _bmad/config.yaml, ask inline once and proceed rather than hard-blocking on module setup.
- (direction) Persona: expert-operator, not facilitator — this skill drives real Zoho/GitHub actions with authority; AskUserQuestion reserved for genuine forks (scope splits, ambiguous merge checks, dangling-session recovery).
- (gap) scan-path-standards.py flags .memlog.md itself as a stray root-level prompt file and flags its own prose for bare _bmad mentions — this is a scanner gap, not a skill defect: working-state-patterns.md specifies .memlog.md sits beside SKILL.md by design, and the memlog is append-only (no edit subcommand) so past entries can't be reworded. Accepted as a known false positive; all real content findings (quick_validate, scan-scripts) are clean.
- (decision) Added a 6th invariant to SKILL.md: never fabricate verification (status/merge/GPG/time-log must correspond to an actual call/check, report tool unavailability/failure rather than inventing a result). Ported from esc-oss-pipeline's 'No fabricated verification' principle, missed in the initial draft — caught while preparing eval cases.
- (event) Created eval fixtures at evals/: cases.json (3 baseline cases — cold-start phase planning, dangling-session resume, issue-vs-work-item distinction), queries.json (11 trigger queries), plus config.yaml/session-state.json fixtures so cases don't stall on missing config.
- (gap) Live eval execution (baseline mode) failed with 'Not logged in - Please run /login' on every case — this session has no ANTHROPIC_API_KEY set (uses subscription/OAuth auth), and the eval sandbox's fresh-HOME subprocess only forwards auth_env when set non-empty on the host, so it can't authenticate. This is an environment/infra limitation, not a gzp-pipeline defect. Eval cases (evals/cases.json, evals/queries.json) are staged and ready to run whenever API-key auth is available. Falling back to the Analyze validation pass instead.
- (event) Ran 5-lens Analyze (Leanness, Architecture, Determinism, Customization, Enhancement) via bmad-workflow-builder's scan-orchestration. 6 findings total: 1 high, 2 medium, 3 low. Determinism and Customization clean.
- (decision) Fixed enhancement-1 (high): open_time_log was a single scalar but issue-report.md promises concurrent sessions (primary task + issue task). Changed schema to open_time_logs keyed by task_id, added dedicated open-time-log/close-time-log commands to session_state.py (each fails loud rather than silently overwriting), updated time-log-session.md, task-loop.md, issue-report.md, and eval fixtures/tests accordingly.
- (decision) Fixed enhancement-2 (medium): current_task.step was declared and branched on in SKILL.md's resume logic but never written anywhere. Wired it at each real checkpoint in task-loop.md (in_progress, pr_open_awaiting_merge, addressing_review_feedback) for both code and non-code paths.
- (decision) Fixed architecture-1 (medium): SKILL.md's config load step referenced bare '.user.yaml' instead of the full {project-root}/_bmad/config.user.yaml path, breaking its own Resolution rules.
- (decision) Fixed architecture-2 (low): task-loop.md's GPG line pointed at 'the GPG invariant in SKILL.md' instead of restating the operative rule inline, violating self-containment (a carved file must stand alone since SKILL.md can drop from context mid-flow). Now states the rule directly.
- (decision) Fixed leanness-1 (low): trimmed the Invariants section's scene-setting preamble ('what flaky looked like before this skill existed') since it didn't change how any bullet is applied. Fixed leanness-2 (low): time-log-session.md stated 'Zoho derives the duration' twice; kept it only at the actionable Stop-step rule.
- (event) Re-ran quick_validate, scan-scripts, prepass-workflow-integrity, and unit tests after fixes — all clean. All 6 Analyze findings resolved.
- (event) analyze: grade excellent, 0 critical / 1 high / 2 medium / 3 low (all fixed pre-ship), report .analysis/2026-08-30-0954/skill-analysis-report.html
- (event) Re-verified enhancement-1's fix against shipped code (schema, commands, references, tests) on request — no changes needed, already correct. Updated skill-analysis-report to note the re-verification pass.
- (event) Create Module (CM): scaffolded as a standalone self-registering module 'Pipeline Forge' (gzp) into /var/home/simon/work/snovak7/bmad-github-zoho-pipeline. Copied gzp-pipeline to repo root, wrote assets/module.yaml and assets/module-help.csv (5 capabilities: configure, start-phase, work-task, report-issue, ad-hoc-request), ran scaffold-standalone-module.py (added assets/module-setup.md, scripts/merge-config.py, scripts/merge-help-csv.py, ../.claude-plugin/marketplace.json), wired module registration check as On Activation step 1 in SKILL.md.
- (gap) module-help.csv row 4 (Work a Task) had an unquoted args field containing a comma ({task: task id/name, or 'next'}), which split into an extra column and failed validate-module.py. Fixed by quoting the field. validate-module.py now passes clean (0 findings).
- (event) Filled marketplace.json owner/author/license/homepage/repository from the destination repo's own git remote (github.com/snovak7/bmad-github-zoho-pipeline) and LICENSE file (MIT) rather than leaving them blank for the user to fill in later.
- (note) scan-scripts.py flags merge-config.py/merge-help-csv.py (2 medium, missing tests) — these are bmad-module-builder's own standalone-module-template scripts, copied verbatim, not authored in this build. Confirmed the same findings exist on the reference bmad-bmb-setup module itself, so this is inherited template characteristic, not a regression to fix here.
